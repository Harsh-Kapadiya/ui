/**
 * Next.js App Router — Route: /about
 *
 * <About> is a pure display component (no hooks) — it can remain a
 * Server Component. No 'use client' directive is needed.
 */
export { About as default } from '../pages/About';
