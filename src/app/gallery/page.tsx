/**
 * Next.js App Router — Route: /gallery
 *
 * 'use client' is required because <Gallery> manages the active filter and
 * the lightbox open/index state via useState.
 */
'use client';
export { Gallery as default } from '../pages/Gallery';
