/**
 * Next.js App Router — Route: /donation
 *
 * 'use client' is required because <Donation> manages form state, selected
 * category, preset amounts, and the success screen via useState.
 */
'use client';
export { Donation as default } from '../pages/Donation';
