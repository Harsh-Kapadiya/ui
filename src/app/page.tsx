/**
 * Next.js App Router — Route: /  (Home)
 *
 * This file is the Next.js page entry point for the root route.
 * No 'use client' is needed here because <Home> contains no browser-only
 * hooks — all its interactive children are already marked 'use client'.
 */
export { Home as default } from './pages/Home';
