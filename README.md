
  # Figma UI Prototype for Temple

  This is a code bundle for Figma UI Prototype for Temple. The original project is available at https://www.figma.com/design/sRwzS7hYTg5JfWgpnVB7fX/Figma-UI-Prototype-for-Temple.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  